# Secure Properties File
# This file contains encrypted configuration properties
# Use the Secure Properties Tool to encrypt sensitive values

# Example encrypted properties (these are placeholders - replace with actual encrypted values)
# sfdc.username=![encrypted_value]
# sfdc.password=![encrypted_value]
# sfdc.token=![encrypted_value]

# Anypoint Platform credentials (encrypted)
# anypoint.platform.client_id=![encrypted_value]
# anypoint.platform.client_secret=![encrypted_value]

# Instructions:
# 1. Use the Secure Properties Tool to encrypt sensitive values
# 2. Replace the placeholder values above with actual encrypted values
# 3. Set the secure.key property in config.properties to decrypt these values at runtime
